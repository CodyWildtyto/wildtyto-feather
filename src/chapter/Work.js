import React from 'react';
import './Work.css';

export default class Work extends React.Component {

    render() {

        return (
                <section id="Work">
                    <h2>Work</h2>
                    <p>Experience of multiple types of web applications. Most of them are built with different features, such as pure JavaScript, React, Vue.js and AngularJS. Here is a selection of my recent work.</p>
                </section>
            );

    }

}